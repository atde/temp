require "Faker"
require "Csv"

class Person
  attr_accessor :first_name, :last_name, :email, :phone, :created_at

  def initialize(first_name, last_name, email, phone, created_at)
    @first_name = first_name
    @last_name = last_name
    @email = email
    @phone = phone
    @created_at = created_at
  end
end

  def personas(n)
    array = []
    n.times do 
      array << Person.new(Faker::Name.first_name,Faker::Name.last_name,Faker::Email.email,Faker.... )


    
  end

